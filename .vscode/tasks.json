{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run Java with File IO",
            "type": "shell",
            "command": "bash",
            "args": [
                "-c",
                "cd \"${fileDirname}\" && javac \"${fileBasename}\" && [ $? -eq 0 ] && java \"${fileBasenameNoExtension}\" < input.txt > output.txt"
            ],
            "presentation": {
                "reveal": "never",
                "close": true
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
